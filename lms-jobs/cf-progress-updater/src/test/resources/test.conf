include "base-test.conf"

kafka {
  input.topic = "flink.cf.progress.input"
  groupId = "flink-cf-progress-updater-group"
}

task {
  consumer.parallelism = 1
  parallelism = 2
}

lms-cassandra {
  keyspace = "cf_progress"
  table = "user_cf_progress"
  host = "localhost"
  port = "9142"
}
